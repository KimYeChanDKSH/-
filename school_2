#include <stdio.h>
#include <stdlib.h>

int main()
{
    int score[20], sum;
    int i;
    for(i = 0; i < 20; i++)
    {
        scanf("%d", &score[i]);
    }
    for(i = 0; i < 20; i++)
    {
        sum += score[i];
    }
    printf("%d", sum);
}
